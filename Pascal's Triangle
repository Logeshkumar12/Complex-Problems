import numpy
def pasc_trngl(n : int):
  for i in range(n):
    for j in range(n-i):
      print(" ",end=" ")
    for j in range(i+1):
      elm=np.factorial(i)//(np.factorial(j)*np.factorial(j-i))
      print(elm,end=" ")
    print()
n=int(input())
pasc_trngl(n)
